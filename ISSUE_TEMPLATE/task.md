---
name: Task
about: A unit of dev work for the queue — written so an agent can act on it as-is
---

<!-- After filing, apply exactly ONE triage label — it is the authority for
     who acts next:
       ready        an agent can produce a reviewable change from this as-is
       needs-spec   design pass first, not code
       needs-trevor only Trevor can do this step
     In vw-scripts, also add a lane: offline-lane or vw-lane.
     Deadlines go in a milestone, never in the title. -->

## What & why

<!-- The problem or need, with enough context that a fresh session needs no
     other source. Name what prompted it. -->

## Acceptance criteria

<!-- What true statements make this closable? Concrete and checkable —
     "the report shows X", not "improve X". -->

## Pointers

<!-- Files, paths, prior issues/PRs, docs, evidence — where to look first. -->

## How to verify

<!-- The command, test, or check that proves the change works. If it can only
     be verified by hand (a VW pass, a feel-check), say so — that usually
     means a lane or needs-trevor label. -->
